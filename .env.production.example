# ==================================
# XTYL Creativity Machine
# Production Environment Variables
# ==================================

# ============ FRONTEND ============
NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# ============ DATABASE ============
DATABASE_URL=postgresql://username:password@db:5432/xtyl_db
POSTGRES_USER=xtyl
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=xtyl_db

# ============ REDIS ============
REDIS_URL=redis://redis:6379/0

# ============ MINIO / S3 ============
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=CHANGE_ME_ACCESS_KEY
MINIO_SECRET_KEY=CHANGE_ME_SECRET_KEY_AT_LEAST_20_CHARS
MINIO_BUCKET=xtyl-storage
# For production, use these for MinIO root credentials:
MINIO_ROOT_USER=CHANGE_ME_ROOT_USER
MINIO_ROOT_PASSWORD=CHANGE_ME_ROOT_PASSWORD_AT_LEAST_20_CHARS

# ============ SECURITY ============
# Generate a strong random secret: openssl rand -hex 32
SECRET_KEY=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_HEX_32
ACCESS_TOKEN_EXPIRE_MINUTES=10080  # 7 days
REFRESH_TOKEN_EXPIRE_DAYS=30       # 30 days

# ============ EMAIL (BREVO) ============
BREVO_API_KEY=xkeysib-YOUR_BREVO_API_KEY
FROM_EMAIL=noreply@yourdomain.com
FROM_NAME=XTYL Creativity Machine

# ============ APPLICATION ============
FRONTEND_URL=https://yourdomain.com

# ============ AI PROVIDERS ============
# OpenAI (optional)
OPENAI_API_KEY=sk-proj-YOUR_OPENAI_API_KEY

# Anthropic Claude (optional)
ANTHROPIC_API_KEY=sk-ant-YOUR_ANTHROPIC_API_KEY

# OpenRouter (recommended - supports multiple models)
OPENROUTER_API_KEY=sk-or-YOUR_OPENROUTER_API_KEY

# Tavily Search (optional - for web search functionality)
TAVILY_API_KEY=tvly-YOUR_TAVILY_API_KEY

# ============ MONITORING (OPTIONAL) ============
# Add Sentry DSN for error tracking
# SENTRY_DSN=https://your-sentry-dsn

# ============ NOTES ============
# 1. NEVER commit this file with real values
# 2. Copy to .env.production and fill with real values
# 3. Keep .env.production in .gitignore
# 4. Use strong passwords (20+ characters)
# 5. Rotate secrets regularly
# 6. Use different credentials for each environment
