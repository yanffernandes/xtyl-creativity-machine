/**
 * Memory Drawer (Stub)
 *
 * TODO: Port full implementation from frontend/src/components/memory/MemoryDrawer.tsx
 */

export function MemoryDrawer({
  open,
}: {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  projectId: string;
}) {
  if (!open) return null;
  return null;
}
