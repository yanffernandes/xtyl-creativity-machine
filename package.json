{
  "name": "xtyl-creativity-machine",
  "private": true,
  "packageManager": "bun@1.3.10",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "dev:api": "bun run --filter @repo/api dev",
    "dev:web": "bun run --filter @repo/web dev",
    "dev:admin": "bun run --filter @repo/admin dev",
    "dev:agno": "uv run --project apps/agno uvicorn main:app --host 0.0.0.0 --port 7777 --reload",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "clean": "turbo clean && rm -rf node_modules .bun-cache"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@dnd-kit/core": "^6.3.1",
    "@dnd-kit/sortable": "^10.0.0",
    "@dnd-kit/utilities": "^3.2.2",
    "@monaco-editor/react": "^4.7.0",
    "@tanstack/react-virtual": "^3.13.19",
    "monaco-editor": "^0.55.1",
    "next-themes": "^0.4.6",
    "react-colorful": "^5.6.1",
    "react-dropzone": "^15.0.0"
  }
}
